// ProgressPage — per-deck SRS stats, daily streak, heatmap
// TODO (Claude Code): cards studied today, streak, per-deck learned/mastered bars,
// session history list, export/import buttons
export default function ProgressPage() {
  return (
    <div>
      <h1 className="font-display text-xl text-ink mb-6">Progress</h1>
      <p className="text-ink/50 text-sm">TODO: SRS stats, streaks, export/import.</p>
    </div>
  )
}
