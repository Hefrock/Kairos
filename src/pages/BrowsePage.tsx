// BrowsePage — deck card grid with SRS status dots
// TODO (Claude Code): grid of mini cards, status dot per card (new/due/learned),
// click to jump to that card in study mode, search/filter by tag
export default function BrowsePage() {
  return (
    <div>
      <h1 className="font-display text-xl text-ink mb-6">Browse Deck</h1>
      <p className="text-ink/50 text-sm">TODO: Card grid with status indicators.</p>
    </div>
  )
}
